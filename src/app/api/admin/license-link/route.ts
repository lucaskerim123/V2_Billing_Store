import {createClient as createSupabaseClient} from "@supabase/supabase-js";
import {masterLicenses} from "@/lib/master-api";
import {licenseDb} from "@/lib/license-api";

const SUPABASE_URL=process.env.NEXT_PUBLIC_SUPABASE_URL||"";
const SUPABASE_KEY=process.env.NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY||"";
const ALLOWED_PRODUCTS=new Set(["orbitfs_base","orbitfs_mcp","orbitfs_apex","orbitfs_studio"]);

async function staff(req:Request){const token=(req.headers.get("authorization")||"").replace(/^Bearer\s+/i,"").trim();if(!token||!SUPABASE_URL||!SUPABASE_KEY)return null;const sb=createSupabaseClient(SUPABASE_URL,SUPABASE_KEY,{global:{headers:{Authorization:"Bearer "+token}},auth:{persistSession:false,autoRefreshToken:false}});const {data:{user},error}=await sb.auth.getUser(token);if(error||!user)return null;const {data}=await sb.rpc("get_my_staff_access");const row=Array.isArray(data)?data[0]:data,p=row?.permissions;const ok=p?.all===true||(Array.isArray(p)?p.includes("licenses.manage")||p.includes("license_api.manage"):Boolean(p?.["licenses.manage"]||p?.["license_api.manage"]));return ok?{user,sb}:null;}
const normalize=(v:any)=>String(v??"").trim().toLowerCase();
const licenseId=(x:any)=>String(x?.id||x?.license_id||x?.licenseId||"").trim();
function matchReason(x:any,c:any,orders:any[]){const number=normalize(c.customer_number),id=normalize(c.id),auth=normalize(c.auth_user_id||c.user_id),email=normalize(c.email),refs=new Set((orders||[]).flatMap((o:any)=>[o.id,o.order_number]).filter(Boolean).map(normalize)),m=x?.metadata||{},vals=[x?.customer_external_id,x?.customer_ref,x?.external_customer_id,x?.customer_id,m.customerNumber,m.customerId,m.customerRecordId,m.customer_external_id,x?.external_reference,x?.order_ref,m.orderRef,m.order_number].map(normalize).filter(Boolean);if(number&&vals.includes(number))return "customer number";if(id&&vals.includes(id))return "customer record";if(auth&&vals.includes(auth))return "account id";if(email&&[normalize(x?.customer_email),normalize(x?.email),normalize(m.customerEmail),normalize(m.email)].includes(email))return "email";if(vals.some(v=>refs.has(v)))return "order";return "";}
export async function GET(req:Request){try{const actor=await staff(req);if(!actor)return Response.json({error:"License management permission required"},{status:403});const customerId=String(new URL(req.url).searchParams.get("customerId")||"").trim();if(!customerId)return Response.json({error:"Customer is required"},{status:400});const {data:customer,error}=await actor.sb.from("customers").select("id,auth_user_id,user_id,customer_number,name,email").eq("id",customerId).maybeSingle();if(error)throw error;if(!customer)return Response.json({error:"Customer not found"},{status:404});const userId=String(customer.auth_user_id||customer.user_id||"").trim();const [b,o,m,i]=await Promise.all([licenseDb().from("license_bindings").select("*").eq("auth_user_id",userId).is("archived_at",null).order("created_at",{ascending:false}),actor.sb.from("orders").select("id,order_number").eq("auth_user_id",userId),masterLicenses(),licenseDb().from("orbitfs_installations").select("id,installation_id,license_binding_id,order_id,order_item_id,component_key,state,release_version,release_id,release_channel,vercel_project_name,vercel_project_id").eq("auth_user_id",userId).order("created_at",{ascending:false})]);if(b.error)throw b.error;if(i.error)throw i.error;const bindings=b.data||[],master=Array.isArray(m?.licenses)?m.licenses:[],installations=i.data||[],linked=new Set(bindings.map((x:any)=>String(x.license_id||"")));const all=master.map((x:any)=>({...x,_match:matchReason(x,customer,o.data||[]),_linked:linked.has(licenseId(x))}));const enrichedBindings=bindings.flatMap((b:any)=>{
  const remote=all.find((x:any)=>licenseId(x)===String(b.license_id||""));
  if(!remote)return [];
  return [{
    ...b,
    authoritative_status:String(remote.status||"unknown"),
    authoritative_expires_at:remote.expires_at||null,
    authoritative_components:remote.components||{},
    master_activations:Array.isArray(remote.activations)?remote.activations:[],
    remote_state:undefined,
    desired_state:undefined
  }];
});return Response.json({customer,bindings:enrichedBindings,installations,licenses:all.filter((x:any)=>x._linked||x._match),allLicenses:all});}catch(e:any){return Response.json({error:e?.message||"Could not load license assignments"},{status:e?.status||500});}}
export async function POST(req:Request){try{const actor=await staff(req);if(!actor)return Response.json({error:"License management permission required"},{status:403});const body=await req.json().catch(()=>({})),customerId=String(body.customerId||"").trim(),wanted=String(body.licenseId||"").trim(),mode=String(body.mode||"manual").trim();if(!customerId)return Response.json({error:"Customer is required"},{status:400});const {data:customer,error}=await actor.sb.from("customers").select("id,auth_user_id,user_id,customer_number,name,email").eq("id",customerId).maybeSingle();if(error)throw error;if(!customer)return Response.json({error:"Customer not found"},{status:404});const userId=String(customer.auth_user_id||customer.user_id||"").trim();if(!userId)return Response.json({error:"Customer has no linked account"},{status:400});const [orders,m]=await Promise.all([actor.sb.from("orders").select("id,order_number").eq("auth_user_id",userId),masterLicenses()]);const master=Array.isArray(m?.licenses)?m.licenses:[];let selected:any=null,reason="";if(mode==="auto"){const matches=master.map((x:any)=>({...x,_match:matchReason(x,customer,orders.data||[])})).filter((x:any)=>x._match);const strong=matches.filter((x:any)=>["customer number","customer record","account id"].includes(x._match));selected=strong.length===1?strong[0]:matches.length===1?matches[0]:null;reason=selected?selected._match:"";if(!selected)return Response.json({error:"No single unambiguous License Master match was found. Use manual linking.",matches:matches.map((x:any)=>({id:licenseId(x),product:x.product_code||x.product||null,reason:x._match}))},{status:409});}else{if(mode!=="manual")return Response.json({error:"Invalid link mode"},{status:400});selected=master.find((x:any)=>licenseId(x)===wanted);if(!selected)return Response.json({error:"License Master licence not found"},{status:404});}const selectedId=licenseId(selected);if(!selectedId)return Response.json({error:"License Master returned a licence without an ID"},{status:502});const product=String(selected.product_code||selected.product||selected.license_product_key||"").toLowerCase();if(!ALLOWED_PRODUCTS.has(product))return Response.json({error:"Unsupported License Master product"},{status:400});const remote=String(selected.status||"active"),key=String(selected.license_key||selected.licenseKey||selected.key||"");const db=licenseDb();const existing=await db.from("license_bindings").select("id,license_id,archived_at").eq("auth_user_id",userId).eq("license_id",selectedId).is("archived_at",null).maybeSingle();if(existing.error)throw existing.error;const owner=await db.from("license_bindings").select("id,auth_user_id").eq("license_id",selectedId).is("archived_at",null).neq("auth_user_id",userId).limit(1).maybeSingle();if(owner.error)throw owner.error;if(owner.data)return Response.json({error:"This License Master licence is already linked to another customer."},{status:409});const terminalStates=new Set(["revoked","expired"]);if(terminalStates.has(remote))return Response.json({error:"A terminal License Master licence cannot be linked. Reissue the licence instead."},{status:409});const currentProduct=await db.from("license_bindings").select("id,license_id").eq("auth_user_id",userId).eq("license_product_key",product).is("archived_at",null).neq("license_id",selectedId).limit(1).maybeSingle();if(currentProduct.error)throw currentProduct.error;if(currentProduct.data)return Response.json({error:"This customer already has an active licence for this product."},{status:409});if(product!=="orbitfs_base"){const base=await db.from("license_bindings").select("id").eq("auth_user_id",userId).eq("license_product_key","orbitfs_base").is("archived_at",null).limit(1).maybeSingle();if(base.error)throw base.error;if(!base.data)return Response.json({error:"OrbitFS Base must be linked before an add-on licence can be linked."},{status:409});}const matchedOrder=(orders.data||[]).find((o:any)=>[o.id,o.order_number].map(normalize).includes(normalize(selected.external_reference||selected.order_ref||"")));const payload={auth_user_id:userId,license_id:selectedId,license_product_key:product,desired_state:remote==="active"?"active":remote,remote_state:remote,license_key_last4:key?key.slice(-4):selected.license_key_last4||null,expires_at:selected.expires_at||null,label:selected.product_name||selected.product_code||selected.product||"OrbitFS licence",api_source:"license_master",admin_override:true,updated_at:new Date().toISOString(),...(matchedOrder?.id?{order_id:matchedOrder.id}:{}),...(selected.order_item_id?{order_item_id:selected.order_item_id}:{}),...(selected.fulfillment_id?{fulfillment_id:selected.fulfillment_id}:{})};const write=existing.data?.id?await db.from("license_bindings").update(payload).eq("id",existing.data.id):await db.from("license_bindings").insert(payload).select("id").single();if(write.error)throw write.error;return Response.json({ok:true,linked:true,licenseId:selectedId,customerId,product,matchReason:reason||"manual",bindingId:existing.data?.id||write.data?.id||null});}catch(e:any){return Response.json({error:e?.message||"License linking failed"},{status:e?.status||500});}}
