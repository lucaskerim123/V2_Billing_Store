import {masterRequest} from "@/lib/master-api";
import {httpError,requireOrbitAdmin} from "@/lib/orbitfs-deployment";

export async function POST(req:Request){try{await requireOrbitAdmin(req);const body=await req.json().catch(()=>({}));const id=String(body.releaseId||body.id||"").trim();if(!id)throw Object.assign(new Error("Release ID is required"),{status:400});const current=await masterRequest(`/api/v1/releases/${encodeURIComponent(id)}`,{method:"GET"},"billing");if(String(current?.release?.release_type||"")!=="update")throw Object.assign(new Error("Billing Store can publish Update releases only"),{status:403});const result=await masterRequest(`/api/v1/releases/${encodeURIComponent(id)}`,{method:"POST",body:JSON.stringify({action:"publish"})},"billing");return Response.json(result,{headers:{"cache-control":"no-store"}})}catch(e){return httpError(e)}}
