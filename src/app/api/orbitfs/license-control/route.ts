import {createClient} from "@/lib/supabase";import {masterRequest} from "@/lib/master-api";
const CUSTOMER_ACTIONS=new Set(["rotate","unlock"]);
export async function POST(req:Request){try{const auth=(req.headers.get("authorization")||"").replace(/^Bearer\s+/i,"").trim();if(!auth)return Response.json({error:"Authentication required"},{status:401});const sb=createClient();const {data:{user},error:userError}=await sb.auth.getUser(auth);if(userError||!user)return Response.json({error:"Authentication expired"},{status:401});const body=await req.json().catch(()=>({})),licenseId=String(body.licenseId||"").trim(),action=String(body.action||"").trim().toLowerCase();if(!licenseId||!CUSTOMER_ACTIONS.has(action))return Response.json({error:"That licence action is not available to customers"},{status:400});const [{data:customer},{data:orders}]=await Promise.all([sb.from("customers").select("id,auth_user_id").eq("auth_user_id",user.id).maybeSingle(),sb.from("orders").select("id,order_number").eq("auth_user_id",user.id)]);const refs=new Set<string>();for(const o of orders||[]){if(o.id)refs.add(String(o.id));if(o.order_number)refs.add(String(o.order_number))}const master=await masterRequest("/api/licenses",{method:"GET"});const licence=(master?.licenses||[]).find((x:any)=>String(x.id||x.license_id)===licenseId&&(String(x.customer_external_id||x.customer_ref||"")===String(user.id)||refs.has(String(x.external_reference||x.order_ref||""))||String(x.customer_external_id||"")===String(customer?.id||"")));if(!licence)return Response.json({error:"Licence not found for this account"},{status:404});const result=await masterRequest(`/api/license/${encodeURIComponent(licenseId)}/control`,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({action,installation_id:body.installationId||null})});const key=result?.key||result?.license?.key||result?.license_key||result?.licenseKey||null;return Response.json({...result,...(key?{key}:{}) ,message:`Licence ${action} request completed.`});}catch(e:any){return Response.json({error:e?.message||"License Master unavailable"},{status:e?.status||502})}}