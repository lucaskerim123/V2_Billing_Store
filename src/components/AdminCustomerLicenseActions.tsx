"use client";
import {useEffect,useMemo,useState} from "react";import {usePathname} from "next/navigation";import {createClient} from "@/lib/supabase";
export default function AdminCustomerLicenseActions(){const path=usePathname(),customerId=useMemo(()=>{const m=path.match(/^\/admin\/customers\/([^/]+)/);return m?m[1]:""},[path]),sb=useMemo(()=>createClient(),[]),[bindings,setBindings]=useState<any[]>([]),[busy,setBusy]=useState(""),[msg,setMsg]=useState("");async function load(){if(!customerId)return;const {data:customer}=await sb.from("customers").select("id,auth_user_id,user_id,customer_number").eq("id",customerId).maybeSingle();const authUserId=String(customer?.auth_user_id||customer?.user_id||customerId);const {data,error}=await sb.from("license_bindings").select("id,license_id,license_product_key,desired_state,remote_state,license_key_last4,label,archived_at").eq("auth_user_id",authUserId).is("archived_at",null).order("created_at",{ascending:false});if(!error)setBindings(data||[])}useEffect(()=>{void load()},[customerId]);async function control(licenseId:string,action:string){if(!licenseId)return;if(!confirm(`Apply ${action} to this licence?`))return;setBusy(action);setMsg("");setNewKey("");const {data:{session}}=await sb.auth.getSession();const r=await fetch(`/api/admin/license-master?path=${encodeURIComponent(`/api/license/${encodeURIComponent(licenseId)}/control`)}`,{method:"POST",headers:{Authorization:`Bearer ${session?.access_token||""}`,"Content-Type":"application/json"},body:JSON.stringify({action})});const j=await r.json().catch(()=>({}));if(!r.ok){setMsg(j.error||`Licence ${action} failed.`);setBusy("");return}const key=j?.key||j?.license?.key||j?.license_key||j?.licenseKey||"";if(action==="rotate"){if(!key){setMsg("Rotation completed but no replacement key was returned.");setBusy("");return}setNewKey(String(key));await sb.from("license_bindings").update({license_key_last4:String(key).slice(-4),updated_at:new Date().toISOString()}).eq("id",b.id)}setBusy("");setMsg(j.message||`Licence ${action} completed.`);await load()}if(!customerId)return null;return <section className="panel" style={{marginBottom:14}}><div className="head"><div><p className="eyebrow">LICENSE CONTROLLER</p><h2>Customer licensing</h2><p className="muted">Customer ID: {customerId}</p><p className="muted">Actions route through License Master.</p></div><a className="buttonlink" href="/admin/licensing">Open License Master</a></div>{newKey&&<div className="notice okBox"><strong>New license key</strong><div className="license-key" style={{wordBreak:"break-all"}}>{newKey}</div><small>Shown once. Save it now.</small><div style={{marginTop:8}}><button onClick={()=>void navigator.clipboard?.writeText(newKey)}>Copy</button><button className="secondary" onClick={()=>setNewKey("")}>Dismiss</button></div></div>}{bindings.length===0?<p className="muted">No active licence bindings found.</p>:bindings.map((b:any)=><div key={b.id} style={{display:"grid",gap:8,padding:"12px 0",borderBottom:"1px solid var(--line,#ddd)"}}><div><b>{b.label||b.license_product_key||"Licence"}</b><span style={{display:"block",opacity:.7,fontSize:12}}>{b.license_id||b.id} · {b.license_key_last4?`••••${b.license_key_last4}`:"Key protected"} · {b.remote_state||b.desired_state||"unknown"}</span></div><div style={{display:"flex",gap:6,flexWrap:"wrap"}}><button disabled={!!busy} onClick={()=>control(b.license_id,"activate")}>Reactivate</button><button disabled={!!busy} onClick={()=>control(b.license_id,"suspend")}>Suspend</button><button disabled={!!busy} onClick={()=>control(b.license_id,"unlock")}>Unlock installations</button><button disabled={!!busy} onClick={()=>control(b.license_id,"rotate")}>Rotate key</button><button className="danger" disabled={!!busy} onClick={()=>control(b.license_id,"terminate")}>Block / terminate</button></div></div>)}{msg&&<p>{msg}</p>}</section>}
